services:
  list:
    - name: "security"
      url: "http://localhost:8081"
      apiKey: ""
    - name: "securityApiKey"
      url: "http://localhost:8082"
      apiKey: "some_secret_key1"


telegram:
  bot:
    enabled: true
    username: "monitoring333_bot"
    token: "8119899012:AAFaOXumNvy5x79UGIIOcqTAfiHZjljF8f8"


springdoc:
  api-docs:
    path: /api-docs
    resolve-schema-properties: true
  swagger-ui:
      path: /swagger-ui.html
      tags-sorter: alpha


spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    url: jdbc:postgresql://localhost:5432/metrics
    password: '1234'
    hikari:
      maximum-pool-size: 10
  jpa:
    open-in-view: 'false'
    hibernate:
      ddl-auto: update
  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql
  application:
    name: monitoringService